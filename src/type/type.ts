export type CartItem = {
    img: string;
    name: string;
    price: string;
};

export type ProductItem = {
    img: string;
    title: string;
    newPrice: string;
    company: string;
    color: string;
    category: string;
}